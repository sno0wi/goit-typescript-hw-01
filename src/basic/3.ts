type StringOrNumber = string | number;

let test: StringOrNumber;

test = 1;
test = 'Max';

type EnableOrdisable = 'enable' | 'disable';

let test_2: EnableOrdisable;

test_2 = 'enable';
test_2 = 'disable';

